'use strict';

describe('Controller Tests', function() {

    describe('Trabajo Detail Controller', function() {
        var $scope, $rootScope;
        var MockEntity, MockTrabajo, MockUser;
        var createController;

        beforeEach(inject(function($injector) {
            $rootScope = $injector.get('$rootScope');
            $scope = $rootScope.$new();
            MockEntity = jasmine.createSpy('MockEntity');
            MockTrabajo = jasmine.createSpy('MockTrabajo');
            MockUser = jasmine.createSpy('MockUser');
            

            var locals = {
                '$scope': $scope,
                '$rootScope': $rootScope,
                'entity': MockEntity ,
                'Trabajo': MockTrabajo,
                'User': MockUser
            };
            createController = function() {
                $injector.get('$controller')("TrabajoDetailController", locals);
            };
        }));


        describe('Root Scope Listening', function() {
            it('Unregisters root scope listener upon scope destruction', function() {
                var eventType = 'plumbeerappApp:trabajoUpdate';

                createController();
                expect($rootScope.$$listenerCount[eventType]).toEqual(1);

                $scope.$destroy();
                expect($rootScope.$$listenerCount[eventType]).toBeUndefined();
            });
        });
    });

});
